# -*- coding: utf-8 -*-"""Created on Mon Oct 11 11:10:14 2021@author: louis.combe"""#centers = np.array([[13,117],[64,28],[82,112],[71,203]]) Rx = 11, Ry = 15 (PING)import numpy as np from Utils.Module.fct_smooth import smoothdef beads_temporal_evolution(frames, centers, Rx = 11, Ry = 15):    results = np.zeros((len(centers), len(frames)))    for i,c in enumerate(centers):        x0,y0 = c        x1,x2,y1,y2 = x0 - Rx, x0 + Rx, y0 - Ry, y0 + Ry         cropped = frames[:,x1:x2,y1:y2]        maxs = np.array([np.max(img) for img in cropped])        results[i,:] = maxs    return results        