# -*- coding: utf-8 -*-"""Created on Fri Oct  8 14:50:00 2021@author: louis.combe"""import numpy as npimport skimage.exposure as exposurefrom Utils.Package.Localisation.module import fast_cam_utils as fcufrom tqdm import tqdm     from skimage.measure import label, regionpropsimport matplotlib.pyplot as pltdef DifferencesVidAcousticWave(path_in=None, frames = None,  thresh = 3, blobsize = 10, equalize = False):     # filename is the output filename and should include .avi        if type(frames) != type(None):        pass    elif type(path_in) != type(None):         frames = fcu.open_multiple_frames(path_in,1,open_all = True ,channel=0)    else:        print('Provide a path or a framelist')        if np.max(frames[0]) > 1:        one = 255    else:        one = 1                if thresh > 1:            thresh = thresh / 255            print(thresh)    frames =frames.astype('float')        img0 = frames[0]    sim = img0.shape    frames_out = []    for j in tqdm(range(1, len(frames) - 1)):         img = frames[j]        img_orig = img.copy()        img = np.abs(img0 - img)        if equalize:            img = exposure.equalize_hist(img)        img1 = frames[j + 1]        img1 = np.abs(img0 - img1)        if equalize:            img1 = exposure.equalize_hist(img1)        img2 = frames[j - 1]        img2 = np.abs(img0 - img2)        if equalize:                img2 = exposure.equalize_hist(img2)        imgL = img > thresh        label_img = label(imgL)        regions = regionprops(label_img)        label_img2=np.zeros_like(label_img)        for region in regions:            if region.area > blobsize:                l = region.label                condition = (label_img == l) & (img1 > thresh) & (img2 > thresh)                img_orig[condition] = one        # for ji in np.arange(1,len(id)+1).reshape(-1):        #     a = STATS(id(ji)).PixelList        #     for i in np.arange(1,a.shape[1-1]+1).reshape(-1):        #         if I(np.array([a(a.shape[1-1] + i)]),np.array([a(i)])) > nl and I1(np.array([a(a.shape[1-1] + i)]),np.array([a(i)])) > nl and I2(np.array([a(a.shape[1-1] + i)]),np.array([a(i)])) > nl:        #             Iorg[np.array[[a[a.shape[1-1] + i]]],np.array[[a[i]]]] = 255        frames_out.append(img_orig)    frames_out = np.array(frames_out)    return frames_out            